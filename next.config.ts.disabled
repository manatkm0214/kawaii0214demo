import type { NextConfig } from "next";

const nextConfig: NextConfig = {`r`n  generateBuildId: async () => null,
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "api.qrserver.com",
      },
    ],
  },
};

export default nextConfig;

